// Compatibility layer - re-export from pantry design system
import { Tooltip } from '../pantry/feedback/Tooltip';

export { Tooltip };

// Legacy compatibility components
export const HelpTooltip = Tooltip;
export const StatusTooltip = Tooltip;